## 📁 Command Category: Nsfw

### 💾 `/nsfw`

**Description:** 🔞 NSFW random content (only in nsfw channel)

### 💻 Usage

`/nsfw <category> <private> [amount]`

### ⚙️ Options

- **`category*`**
  - **Description:** Content category
  - **Type:** Text
  - **Choices:** `hass` (`hass`), `hmidriff` (`hmidriff`), `pgif` (`pgif`), `4k` (`4k`), `hentai` (`hentai`), `holo` (`holo`), `hneko` (`hneko`), `neko` (`neko`), `hkitsune` (`hkitsune`), `kemonomimi` (`kemonomimi`), `anal` (`anal`), `hanal` (`hanal`), `gonewild` (`gonewild`), `kanna` (`kanna`), `ass` (`ass`), `pussy` (`pussy`), `thigh` (`thigh`), `hthigh` (`hthigh`), `coffee` (`coffee`), `food` (`food`), `paizuri` (`paizuri`), `tentacle` (`tentacle`), `boobs` (`boobs`), `hboobs` (`hboobs`), `yaoi` (`yaoi`)
- **`private*`**
  - **Description:** Make the message private?
  - **Type:** Boolean
- **`amount`**
  - **Description:** Amount of images to send (1-3)
  - **Type:** Integer


